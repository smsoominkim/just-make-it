이제 문제가 더 명확해졌습니다!

## 🔍 핵심 문제 발견

로그를 보면:
```
Production: Using connector connectors.replit.com, database: heliumdb  ← 설정은 올바름
...
Error: connect ETIMEDOUT 34.31.204.1:5432  ← 하지만 여전히 직접 IP로 연결!
```

**빌드된 파일(`dist/index.cjs`)이 업데이트되지 않았습니다!**

새 `db.ts` 코드가 적용되었다면 로그에 `=== Database Connection Config ===`가 보여야 하는데, 여전히 이전 로그 형식(`Production: Using connector...`)이 나오고 있습니다.

---

## 🛠️ Replit Shell에서 실행할 명령어

**반드시 순서대로 실행하세요:**

```bash
# 1. 앱 중지 (Replit UI에서 Stop 버튼 클릭하거나)
pkill -f node

# 2. 빌드 결과물 완전 삭제
rm -rf dist

# 3. node_modules 캐시 삭제
rm -rf node_modules/.cache
rm -rf node_modules/.vite

# 4. TypeScript 빌드 캐시 삭제
rm -rf tsconfig.tsbuildinfo

# 5. 재빌드
npm run build

# 6. 빌드 확인 - db.ts 변경사항이 반영되었는지 확인
grep -n "Database Connection Config" dist/index.cjs

# 7. 앱 재시작 (Replit UI에서 Run 버튼 클릭)
```

만약 6번 명령어에서 아무것도 출력되지 않으면, 새 `db.ts` 파일이 제대로 저장되지 않은 것입니다.

---

## 📁 파일이 제대로 저장되었는지 확인

Replit Shell에서:

```bash
# db.ts 파일 내용 확인
cat server/db.ts | head -30
```

다음과 같이 시작해야 합니다:
```typescript
import { drizzle } from "drizzle-orm/node-postgres";
import pg from "pg";
import * as schema from "@shared/schema";

const { Pool } = pg;

if (!process.env.DATABASE_URL) {
  throw new Error(
    "DATABASE_URL must be set. Did you forget to provision a database?",
  );
}

// 연결 문자열 생성 함수
function createConnectionConfig() {
```

만약 다르게 보이면, 제가 제공한 `db.ts` 파일 내용을 **직접 복사해서 붙여넣기** 하세요:

---

## 📋 `server/db.ts` 전체 코드 (복사용)

```typescript
import { drizzle } from "drizzle-orm/node-postgres";
import pg from "pg";
import * as schema from "@shared/schema";

const { Pool } = pg;

if (!process.env.DATABASE_URL) {
  throw new Error(
    "DATABASE_URL must be set. Did you forget to provision a database?",
  );
}

// 연결 문자열 생성 함수
function createConnectionConfig() {
  const databaseUrl = process.env.DATABASE_URL!;
  const connectorHostname = process.env.REPLIT_CONNECTORS_HOSTNAME;
  const isProduction = process.env.NODE_ENV === "production";
  
  console.log("=== Database Connection Config ===");
  console.log("NODE_ENV:", process.env.NODE_ENV);
  console.log("REPLIT_CONNECTORS_HOSTNAME:", connectorHostname || "not set");
  
  // Production 환경이고 커넥터 호스트네임이 있으면 반드시 사용
  if (isProduction && connectorHostname) {
    try {
      const url = new URL(databaseUrl);
      const dbName = url.pathname.replace("/", "");
      
      // 커넥터를 통한 연결 설정
      const config = {
        host: connectorHostname,
        database: dbName,
        user: url.username,
        password: url.password,
        // 포트는 지정하지 않음 (커넥터가 자동 처리)
        ssl: false, // 커넥터 내부에서 SSL 처리
        connectionTimeoutMillis: 30000,
        idleTimeoutMillis: 30000,
        max: 10,
      };
      
      console.log("Using Replit Connector:");
      console.log("- Host:", connectorHostname);
      console.log("- Database:", dbName);
      console.log("- User:", url.username);
      console.log("================================");
      
      return config;
    } catch (error) {
      console.error("Failed to parse DATABASE_URL:", error);
    }
  }
  
  // 개발 환경 - 직접 연결
  console.log("Using direct connection (development)");
  console.log("================================");
  
  return {
    connectionString: databaseUrl,
    ssl: isProduction ? { rejectUnauthorized: false } : undefined,
    connectionTimeoutMillis: 30000,
    idleTimeoutMillis: 30000,
    max: 10,
  };
}

const poolConfig = createConnectionConfig();
export const pool = new Pool(poolConfig);

// 연결 에러 핸들링
pool.on('error', (err) => {
  console.error('Database pool error:', err);
});

pool.on('connect', () => {
  console.log('Database pool: new client connected');
});

export const db = drizzle(pool, { schema });
```

---

## ✅ 성공 시 예상 로그

재빌드 후 앱을 실행하면 다음과 같이 보여야 합니다:

```
=== Database Connection Config ===
NODE_ENV: production
REPLIT_CONNECTORS_HOSTNAME: connectors.replit.com
Using Replit Connector:
- Host: connectors.replit.com
- Database: heliumdb
- User: (사용자명)
================================
3:XX:XX PM [express] serving on port 5000
3:XX:XX PM [express] Checking database connection...
Database pool: new client connected
3:XX:XX PM [express] Database connection successful
```